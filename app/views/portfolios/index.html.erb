<h1> Portfolio Items</h1>

<% @portolio_items.each do |portfolio_item| %>
  <p><%= portfolio_item.title %></p>
  <p><%= portfolio_item.subtitle %></p>
  <p><%= portfolio_item.body %></p>
  <%= portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %></p>
<% end %>